<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link href="/stylesheets/style.css" rel="stylesheet" type="text/css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@100&display=swap"
      rel="stylesheet"
    />
    <script src="https://kit.fontawesome.com/5deca06548.js" crossorigin="anonymous"></script>
    <title>Members Only</title>
  </head>
  <body>
    <nav class="navBar">
      <% if (user) {%>
      <div class="loggedInWrapper">
        <div class="loggedInUserName">
          Welcome back, <%= user.username %>
        </div>
        <div>
          <a href="/create-post" class="makePost">Post</a>
        </div>
        <!-- logged in? not a member? -->
          <button class="secret-button">
            <img src="/public/images/question-mark.png" alt="question mark"></img>
          </button>
          <a href="/log-out" class="logOutButton">Log out</a>
      </div>
      <% } else { %>
      <form action="/log-in" method="POST">
        <div class="navItem">
          <label for="username" class="label">Username</label>
          <input
            name="username"
            placeholder="Username"
            type="text"
            class="usernameInput"
          />
        </div>
        <div class="navItem">
          <label for="password" class="label">Password</label>
          <input
            name="password"
            type="password"
            class="passwordInput"
            placeholder="Password"
          />
          <button class="submitLogIn">Log In</button>
          
        </div>
        <div class="navItem">
          <button class="submitLogIn"><a href="/sign-up">Sign Up</a></button>
        </div>
      </form>
      <%}%>
    </nav>
    <div class="wrapper">
      <!-- will be a loop containing all messages -->
      <% for(var i = 0; i < data.length; i++) {%>
      <div class="message-container">
        <div class="message-title"><%= data[i].title %></div>
        <div class="message-body">
          <%= data[i].body %>
        </div>
        <div class="message-timestamp"><%= data[i].added %></div>
        <div class="message-data">
          <% if (user && user.isMember === true) {%>
          <%= data[i].user %>
          <%}%>
        </div>
      </div>
      <% } %>
    <div class="overlay"></div>
    <div class="backgroundHero"></div>
  </body>
</html>
